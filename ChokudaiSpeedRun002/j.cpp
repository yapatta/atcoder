#include <bits/stdc++.h>
using namespace std;
using ll = long long;
inline ll gcd(ll a, ll b) {
  if(a==0) return b;
  if(a>b) return gcd(a%b, b);
  else return gcd(b%a, a);
}

int main(){
	 ll n;
	 cin >> n;
	 ll ans = 0;
	 vector<ll> dp(n+1);
	 dp[]
	 for(ll i=0;i<n;i++) 
	 {
		 ll a,b;
		 cin >> a >> b;
	 }
}
