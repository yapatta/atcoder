#include <iostream>
#include <string>
#include <algorithm>
#include <cmath>
#include <numeric>
#include <vector>
#include <queue>
#include <map>
typedef unsigned long long ull;
typedef long long ll;
#define MAX 3005
using namespace std;
ll A[MAX];
ll max=0;
int main() {
  int i,n, q;
  cin >> n >> q;
  for(i=0;i<n;i++) cin >> A[i];
  for(i=0;i<q;i++) cin >> X[i] >> Y[i];
  
  return 0;
}
